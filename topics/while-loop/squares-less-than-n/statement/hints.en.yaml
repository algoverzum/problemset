- Instead of checking which numbers are squares, let's generate square numbers until we reach n.
- Because we don't know how many smaller than n square numbers there are, let's use a while loop.
- cpp: |
    `while (i * i < n) { ... }`
  py: |
    `while i * i < n: ...`
