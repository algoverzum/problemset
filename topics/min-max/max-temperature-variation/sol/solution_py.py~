#!/usr/bin/env python3
# @check-accepted: *

n = int(input())
A = [int(x) for x in input().split()]
B = [int(x) for x in input().split()]

res = B[0] - A[0]

for i in range(1,N):
    current = B[i] - A[i]
    if current > res:
        res = current

print(res)
