---
- cpp: We store the distances and prices of the trips in a vector. As we go through them (using index), we save the cheapest trip was so far for at least $K$ distance.
  py: We store the distances and prices of the trips in a list. As we go through them (using index), we save the cheapest trip was so far for at least $K$ distance.
- Let us have a `mini` variable, initially set to -1. In a for loop, examine the items and if the current distance is at least $K$, update the value of `mini`. What exactly should be the condition?
- If the value of `mini` is still -1, then we should set it to the current price, otherwise if the current price is less than `mini` then we should update it too.
