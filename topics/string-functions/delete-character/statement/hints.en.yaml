---
- cpp: Iterate through the characters of the string, and if the current character does not match the one to be removed, print it.
  py: Use the `s.replace()` function. Replace the specified character with an empty string ("").
