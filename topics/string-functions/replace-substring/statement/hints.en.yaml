---
- cpp: Iterate through the string and examine its 4-character substrings (`s.substr(i, 4)`). If it matches the target substring, replace the characters. Unfortunately, we can only modify it character by character, but for 4 characters, it's not too bad.
  py: Use the `s.replace()` function.
