- Count how many of the listed ages is least 18 and at most 21 years. And divide the number of sufficent ages by 3 to get how many teams they can form.
- cpp: |
      To get if an age meets the requirements you can use:
      if(age >= 18 && age < 21)
- py: |
      To get if an age meets the requirements you can use:
      if age >= 18 and age < 21:
