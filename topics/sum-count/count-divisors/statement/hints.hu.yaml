- Using a `for` loop, go through the numbers from i=1 to N. For each number i, check whether it is a divisor of N. How can this be done? And what do we do then?
- Within the `for` loop, we need an `if` statement to test the divisibility, and the correct condition is `n % i == 0`. If this is true, then increase a counter variable, which must be created before the loop with an initial value of 0. 