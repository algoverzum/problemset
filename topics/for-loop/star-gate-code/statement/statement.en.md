## Star Gate Code
To depart from our planet, you have to open the star gate using a *four-digit* secret code. It has not been opened in decades. All we remember is that the code is a round number: ends in two zeros, and it doesn't start with a 0. Can you list all the possible values of the code? Print the values in increasing order, each number in a separate line.
