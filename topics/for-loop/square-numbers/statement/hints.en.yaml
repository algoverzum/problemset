- Instead of square numbers we should use a `for` cycle to go through numbers from $i=1$ to $N$. (The square of the number $i$ is $i*i$.)
- cpp: Our for cycle could be: `for (int i = 1; i <= N; i++) { ... }`
  py: Our for cycle could be: `for i in range(1, N + 1):`
