- We should go through the numbers from $i=1$ to $N$. If $N$ is divisible by $i$, then we should print it.
- cpp: The `if` inside the `for` cycle could be `if (N % i == 0) {cout << i << "\n";}` 
  py: The `if` inside the `for` cycle could be `if N % i == 0: print(i)`