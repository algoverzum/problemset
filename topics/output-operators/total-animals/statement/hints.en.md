### Hint 1
In C++ you can print to the standard output using the `cout` output stream and the `<<` operator, for example, the `cout << 42;` command prints the number 42 to the output.

### Hint 2
The computer can perform mathematical calculations easily. Just use the + operator as you would write down the calculation on a paper.

### Hint 3
    cout << 13337 + 42424;
